apply plugin: 'java'

dependencies {
	compile fileTree( dir: "../libs", include: ['JUtils.jar', 'TerminalWindow.jar'] )
}

ext {
	jarName = "WakeOnLAN"
	includeLibs = true
	copyLibs = true
	deployedProjDir = "deployed/Tools"
	deployedLibDir = "./"
	libDest = "deployed/Tools/libs"
	jarPathPrefix = "./libs/"
	
	mainClass = "app.WOLMonitor"
	iconFile = 'WoL.ico'
}

apply from: '../gradle/utilapp.gradle'